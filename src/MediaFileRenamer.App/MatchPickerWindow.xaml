<Window x:Class="MediaFileRenamer.App.MatchPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Match" Height="680" Width="960" MinHeight="560" MinWidth="820"
        WindowStartupLocation="CenterOwner" Background="#F4F6F8">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Choose the correct match" FontSize="24" FontWeight="SemiBold" Foreground="#20262E"/>
            <TextBlock x:Name="SourceTextBlock" Foreground="#566171" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="SearchTextBox" MinHeight="34" VerticalContentAlignment="Center"
                     KeyDown="SearchTextBox_KeyDown"/>
            <Button Grid.Column="1" Content="Search" Width="90" MinHeight="34" Margin="8,0,0,0" Click="Search_Click"/>
        </Grid>

        <Grid Grid.Row="2">
            <ListBox x:Name="CandidateList" ItemsSource="{Binding Candidates}" MouseDoubleClick="CandidateList_MouseDoubleClick"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderBrush="#CCD3DA">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#E1E5EA" BorderThickness="0,0,0,1" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="92"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="78" Height="116" Background="#E7EBF0" BorderBrush="#CDD4DC" BorderThickness="1">
                                <Image Source="{Binding PosterUrl}" Stretch="UniformToFill"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#20262E"
                                               TextWrapping="Wrap" MaxWidth="600"/>
                                    <TextBlock Text="{Binding Year, StringFormat=  ({0})}" FontSize="16" Foreground="#566171" Margin="6,2,0,0"/>
                                </StackPanel>
                                <TextBlock Text="{Binding MediaType}" Foreground="#3F6E9A" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding Overview}" TextWrapping="Wrap" Foreground="#566171" Margin="0,8,0,0"
                                           MaxHeight="64" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
            <TextBlock x:Name="SearchStatusTextBlock" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="#566171" FontSize="16" TextWrapping="Wrap" TextAlignment="Center"
                       MaxWidth="520" IsHitTestVisible="False"/>
        </Grid>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <Button Content="Use as Movie" Width="110" MinHeight="34" Margin="0,0,8,0" Click="UseAsMovie_Click"/>
                <Button Content="Use as TV" Width="100" MinHeight="34" Click="UseAsTv_Click"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Cancel" Width="90" MinHeight="34" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="Use Selected" Width="120" MinHeight="34" Click="UseSelected_Click"/>
            </StackPanel>
            <TextBlock Text="Double-click a result to choose it." VerticalAlignment="Center" Foreground="#566171"/>
        </DockPanel>
    </Grid>
</Window>
