# Media File Renamer 1.1.1 acceptance record

Release-candidate acceptance completed on Windows on 2026-08-12 using the
self-contained Windows x64 application and synthetic media fixtures. Provider
checks used tester-owned credentials through the application UI; no credential
values are recorded here or in the acceptance artifacts.

## Passed gates

- Release build completed with zero warnings and all 169 automated tests passed.
- The packaged candidate passed 58 UI checks at the active 125% display scale,
  including visible `Review needed` explanation and resolution actions.
- TMDB and TVDB connection tests passed using personal credentials.
- A non-standard `Episode 16 The Beast Is Awake in Bottomless` filename resolved
  to `Scooby-Doo, Where Are You! - S03E16 - The Beast is Awake in Bottomless Lake`.
- The existing Blade Runner search, Curious George S00E07/S00E08, TVDB-order
  fallback, and diagnostic redaction provider checks remained green.
- Review-resolution tests cover conservative sibling-season inference,
  high-confidence partial-title matching, ambiguous-title rejection, episode
  ranking, manual confirmation, and incomplete-manual-input rejection.
- Local copy, move, Undo, timestamp preservation, collision blocking,
  cancellation, crash recovery, and idempotent relaunch scenarios passed.
- Copy, move/Undo, network interruption, unavailable-share startup, and restored
  recovery scenarios passed against a uniquely named disposable SMB share.

## Scope notes

- The review and matching release does not change the transactional transfer
  layer, but both local and disposable-UNC transfer/recovery matrices were
  rerun successfully in addition to the complete automated regression suite.
- Full Narrator workflow certification remains optional and is not a release
  gate. Keyboard navigation, automation names, constrained layouts, theme
  resources, and High Contrast resources remain automated checks.
- The release is an unsigned portable Windows x64 ZIP. Each user supplies their
  own TMDB and TVDB credentials; the package contains no provider credentials.
